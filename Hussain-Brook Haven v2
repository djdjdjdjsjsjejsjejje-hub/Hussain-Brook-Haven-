-- Hussein Hub V2 (2026 WindUI Edition)
-- The Ultimate Edition: Exploits, Anti-Flings & Vehicles Unlocked!
-- تحديث مخصص: قسم نحن نحترم محرم 🏴

local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local Window = WindUI:CreateWindow({
   Title = "Hussein Hub V2",
   SubTitle = "Brookhaven RP (2026)",
   Author = "by hussain_90_1",
   Size = UDim2.fromOffset(580, 500),
   Transparent = true
})

-- وظيفة جلب أسماء اللاعبين
local function getPlayerNames()
   local names = {}
   for _, p in pairs(game.Players:GetPlayers()) do
      if p ~= game.Players.LocalPlayer then 
         table.insert(names, p.Name) 
      end
   end
   return names
end

-- وظيفة جلب الوقت الحالي من جهازك العراقي مباشرة (نظام 12 ساعة مريح)
local function getIraqTime()
   local localTime = os.date("*t") -- يقرأ وقت جهازك في العراق فوراً بدون زيادة
   local hour = localTime.hour
   local period = "AM"
   
   if hour >= 12 then
      period = "مساءً"
      if hour > 12 then
         hour = hour - 12
      end
   else
      period = "صباحاً"
      if hour == 0 then
         hour = 12
      end
   end
   
   return string.format("%02d:%02d:%02d %s", hour, localTime.min, localTime.sec, period)
end

-- وظيفة جلب التاريخ من جهازك العراقي مباشرة
local function getIraqDate()
   return os.date("%Y/%m/%d") -- يقرأ تاريخ جهازك فوراً
end

----------------------------------------------------
----------------------------------------------------
-- [6] قسم حقوق المطور (حسين) 👑
----------------------------------------------------
local TabCredits = Window:Tab({ Title = "حقوق المطور", Icon = "rbxassetid://4483362458" })

TabCredits:Section({ Title = "📱 حسابات المطور الرسمية" })

TabCredits:Button({
   Title = "👤 تيك توك: hussain_90_1",
   Callback = function()
      -- نسخ الحساب تلقائياً لجهاز اللاعب
      setclipboard("hussain_90_1")
      WindUI:Notify({ 
         Title = "تم النسخ!", 
         Content = "تم نسخ يوزر التيك توك بنجاح، يمكنك الآن البحث عنه ومتابعته.", 
         Duration = 4 
      })
   end
})

TabCredits:Button({
   Title = "🎮 روبلوكس: Hussain4646767",
   Callback = function()
      -- نسخ الحساب تلقائياً لجهاز اللاعب
      setclipboard("Hussain4646767")
      WindUI:Notify({ 
         Title = "تم النسخ!", 
         Content = "تم نسخ يوزر الروبلوكس بنجاح، أرسل لي طلب صداقة!", 
         Duration = 4 
      })
   end
})

TabCredits:Section({ Title = "📝 إصدار السكربت الحصري" })
TabCredits:Button({
   Title = "Hussein Hub V2 - 2026 WindUI Edition",
   Callback = function()
      WindUI:Notify({ 
         Title = "شكر خاص", 
         Content = "شكراً لاستخدامك سكربت حسين المطور V2 لـ بروك هافن.", 
         Duration = 5 
      })
   end
})

----------------------------------------------------
-- [2] قسم اختراق الجدران والمضادات (V2)
----------------------------------------------------
local TabExploits = Window:Tab({ Title = "الحماية والاختراق", Icon = "rbxassetid://4483362458" })

TabExploits:Section({ Title = "🔓 ميزة اختراق الجدران" })

local noclipEnabled = false
local noclipConnection
TabExploits:Toggle({
   Title = "تفعيل اختراق الجدران المستمر (NoClip)",
   Value = false,
   Callback = function(state)
      noclipEnabled = state
      if noclipEnabled then
         WindUI:Notify({ Title = "NoClip V2", Content = "تم تفعيل اختراق الجدران بنجاح!", Duration = 3 })
         noclipConnection = game:GetService("RunService").Stepped:Connect(function()
            if noclipEnabled and game.Players.LocalPlayer.Character then
               for _, part in pairs(game.Players.LocalPlayer.Character:GetChildren()) do
                  if part:IsA("BasePart") then part.CanCollide = false end
               end
            end
         end)
      else
         if noclipConnection then noclipConnection:Disconnect() end
         WindUI:Notify({ Title = "NoClip V2", Content = "تم إيقاف اختراق الجدران.", Duration = 3 })
      end
   end
})

TabExploits:Section({ Title = "🛡️ مضادات الطيران والكنب والسفن" })

local antiFlingEnabled = false
TabExploits:Toggle({
   Title = "مضاد الطيران والفلنق (سفينة، كنبة، أغراض طائرة)",
   Value = false,
   Callback = function(state)
      antiFlingEnabled = state
      if antiFlingEnabled then
         WindUI:Notify({ Title = "Anti-Fling", Content = "حماية الطيران الإجباري تعمل الآن!", Duration = 3 })
         task.spawn(function()
            while antiFlingEnabled do
               local char = game.Players.LocalPlayer.Character
               local hrp = char and char:FindFirstChild("HumanoidRootPart")
               local hum = char and char:FindFirstChildOfClass("Humanoid")
               
               if hrp and hum then
                  if hrp.Velocity.Magnitude > 70 or hrp.RotVelocity.Magnitude > 70 then
                     hrp.Velocity = Vector3.new(0, 0, 0)
                     hrp.RotVelocity = Vector3.new(0, 0, 0)
                  end
                  if hum.SeatPart then
                     hum.Sit = false
                     hrp.CFrame = hrp.CFrame + Vector3.new(0, 3, 0)
                  end
               end
               task.wait(0.1)
            end
         end)
      end
   end
})

local antiStuckEnabled = false
TabExploits:Toggle({
   Title = "مضاد اللصق والتعليق في الأرض (Anti-Anchor)",
   Value = false,
   Callback = function(state)
      antiStuckEnabled = state
      if antiStuckEnabled then
         task.spawn(function()
            while antiStuckEnabled do
               local char = game.Players.LocalPlayer.Character
               if char then
                  for _, part in pairs(char:GetChildren()) do
                     if part:IsA("BasePart") and part.Anchored then part.Anchored = false end
                  end
               end
               task.wait(0.2)
            end
         end)
      end
   end
})

----------------------------------------------------
-- [3] قسم السيارات الكامل (V2) 🏎️
----------------------------------------------------
local TabVehicles = Window:Tab({ Title = "قسم السيارات", Icon = "rbxassetid://4483362458" })

TabVehicles:Section({ Title = "🚗 رسبون وتحكم بالمركبات" })

local carList = {"Tesla", "Ferrari", "Mustang", "Jeep", "Supra", "Lamborghini", "Ambulance", "Police Car"}
local selectedCar = "Tesla"

TabVehicles:Dropdown({
   Title = "اختر السيارة المراد رسبنتها",
   Values = carList,
   Callback = function(value) selectedCar = value end
})

TabVehicles:Button({
   Title = "🏎️ رسبون السيارة المختارة فوراً",
   Callback = function()
      pcall(function()
         game:GetService("ReplicatedStorage").SpawnVehicle:FireServer(selectedCar)
         WindUI:Notify({ Title = "Car Spawned", Content = "تم رسبنة سيارتك بنجاح!", Duration = 3 })
      end)
   end
})

TabVehicles:Section({ Title = "⚡ هكر السرعة الخارقة للسيارات" })

TabVehicles:Button({
   Title = "🚀 جعل سرعة سيارتك الحالية 200 (تعديل العزم الحقيقي)",
   Callback = function()
      local char = game.Players.LocalPlayer.Character
      local humanoid = char and char:FindFirstChildOfClass("Humanoid")
      if humanoid and humanoid.SeatPart and humanoid.SeatPart:IsA("VehicleSeat") then
         local seat = humanoid.SeatPart
         seat.MaxSpeed = 200
         seat.Torque = 30000 
         seat.TurnSpeed = 25 
         WindUI:Notify({ Title = "Speed Hack Active", Content = "تم تعديل المحرك بنجاح (V2)!", Duration = 4 })
      else
         WindUI:Notify({ Title = "تنبيه!", Content = "يجب أن تكون راكباً داخل مقعد السائق أولاً!", Duration = 4 })
      end
   end
})

----------------------------------------------------
-- [4] قسم تعديلات اللاعب ومراقبة السيرفر (V2)
----------------------------------------------------
local TabPlayer = Window:Tab({ Title = "اللاعب والمراقبة", Icon = "rbxassetid://4483362458" })

TabPlayer:Section({ Title = "👟 تعديلات السرعة شورت" })
TabPlayer:Button({
   Title = "Super WalkSpeed (سرعة شخصية خارقة)",
   Callback = function()
      if game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
         game.Players.LocalPlayer.Character:FindFirstChildOfClass("Humanoid").WalkSpeed = 300
      end
   end
})

TabPlayer:Section({ Title = "👁️ مراقبة السيرفر واللاعبين" })
local DropdownAR = TabPlayer:Dropdown({
   Title = "اختر لاعب لمراقبته عن بعد",
   Values = getPlayerNames(),
   Callback = function(Option)
      if Option and Option ~= "" then
         local target = game.Players:FindFirstChild(Option)
         if target and target.Character and target.Character:FindFirstChildOfClass("Humanoid") then
            workspace.CurrentCamera.CameraSubject = target.Character:FindFirstChildOfClass("Humanoid")
         end
      end
   end
})

TabPlayer:Button({ Title = "🔄 تحديث قائمة اللاعبين", Callback = function() DropdownAR:Refresh(getPlayerNames()) end })
TabPlayer:Button({ Title = "❌ إلغاء المراقبة والعودة لشخصيتك", Callback = function() workspace.CurrentCamera.CameraSubject = game.Players.LocalPlayer.Character:FindFirstChildOfClass("Humanoid") end })
----------------------------------------------------
-- [7] قسم نسخ السكنات (Avatar Copier) 🎭
----------------------------------------------------
local TabCopyOutfit = Window:Tab({ Title = "نسخ السكنات", Icon = "rbxassetid://4483362458" })

TabCopyOutfit:Section({ Title = "🎭 قلد وانسخ سكنات اللاعبين" })

-- وظيفة لتطبيق مظهر لاعب آخر على شخصيتك
local function copyPlayerOutfit(targetName)
   local targetPlayer = game.Players:FindFirstChild(targetName)
   local localPlayer = game.Players.LocalPlayer
   
   if targetPlayer and targetPlayer.Character and localPlayer.Character then
      local localHumanoid = localPlayer.Character:FindFirstChildOfClass("Humanoid")
      
      if localHumanoid then
         -- جلب وصف المظهر (Description) الخاص باللاعب المستهدف وتطبيقه
         local pcallSuccess, outfitDescription = pcall(function()
            return game.Players:GetHumanoidDescriptionFromUserId(targetPlayer.UserId)
         end)
         
         if pcallSuccess and outfitDescription then
            localHumanoid:ApplyDescription(outfitDescription)
            WindUI:Notify({ 
               Title = "Hussein Hub V2", 
               Content = "تم نسخ سكن اللاعب " .. targetName .. " بنجاح!", 
               Duration = 4 
            })
         else
            WindUI:Notify({ Title = "خطأ!", Content = "فشل جلب بيانات مظهر اللاعب!", Duration = 3 })
         end
      end
   else
      WindUI:Notify({ Title = "تنبيه!", Content = "اللاعب غير موجود أو شخصيتك غير جاهزة!", Duration = 3 })
   end
end

-- 1. خيار اختيار لاعب من القائمة لنسخ سكنه
local DropdownOutfit = TabCopyOutfit:Dropdown({
   Title = "اختر لاعب من السيرفر لنسخ مظهرة",
   Values = getPlayerNames(),
   Callback = function(Option)
      if Option and Option ~= "" then
         copyPlayerOutfit(Option)
      end
   end
})

-- زر لتحديث قائمة الأسماء في القائمة المنسدلة
TabCopyOutfit:Button({ 
   Title = "🔄 تحديث قائمة اللاعبين لنسخ السكن", 
   Callback = function() 
      DropdownOutfit:Refresh(getPlayerNames()) 
   end 
})

TabCopyOutfit:Section({ Title = "✍️ نسخ السكن عن طريق الكتابة" })

-- 2. خيار كتابة اسم اللاعب يدوياً (في حال لم يظهر في القائمة)
local targetInputName = ""
TabCopyOutfit:Input({
   Title = "اكتب اسم اللاعب الكامل (Username)",
   Placeholder = "مثال: Hussain4646767",
   Callback = function(text) 
      targetInputName = text 
   end
})

TabCopyOutfit:Button({
   Title = "🎭 اطلب مظهر الاسم المكتوب الآن",
   Callback = function()
      if targetInputName ~= "" then
         copyPlayerOutfit(targetInputName)
      else
         WindUI:Notify({ Title = "تنبيه!", Content = "الرجاء كتابة اسم لاعب أولاً!", Duration = 3 })
      end
   end
})

----------------------------------------------------
-- [5] قسم نحن نحترم محرم والتوقيت العراقي 🏴
----------------------------------------------------
local TabMuharram = Window:Tab({ Title = "نحن نحترم محرم", Icon = "rbxassetid://4483362458" })

TabMuharram:Section({ Title = "⏰ ميقات جمهورية العراق المباشر" })

TabMuharram:Button({
   Title = "عرض تاريخ ووقت العراق الحالي",
   Callback = function()
      local currentTime = getIraqTime()
      local currentDate = getIraqDate()
      WindUI:Notify({ 
         Title = "توقيت بغداد المباشر", 
         Content = "التاريخ: " .. currentDate .. "\nالساعة الآن: " .. currentTime, 
         Duration = 6 
      })
   end
})

TabMuharram:Section({ Title = "🏴 عبارات الاحترام والتقدير" })

TabMuharram:Button({
   Title = "إظهار إشعار احترام الشهر الحرام",
   Callback = function()
      WindUI:Notify({ 
         Title = "نحن نحترم محرم 🏴", 
         Content = "عظم الله أجورنا وأجوركم بمصاب أبا عبد الله الحسين عليه السلام.", 
         Duration = 7 
      })
      pcall(function()
         -- تعديل تلقائي محترم للاسم والبايو ليناسب المناسبة
         game:GetService("ReplicatedStorage").UpdateName:FireServer("<font color='#FF0000'>يا حسين</font>")
         game:GetService("ReplicatedStorage").UpdateBio:FireServer("<font color='#000000'>نحن نحترم محرم - Hussein Hub V2</font>")
      end)
   end
})
